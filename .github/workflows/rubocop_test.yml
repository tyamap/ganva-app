name: Rubocop

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ruby:2.6.4
      env:
        RAILS＿ENV: test
    steps:
      - uses: actions/checkout@v1
      - name: Prepare bundler
        run: |
          gem install bundler
          bundle install --jobs 4 --retry 3
      - name: Run rubocop
        run: |
          bundle exec rubocop