<%= markup do |m|
  p = UserFormPresenter.new(f, self)
  m << p.notes
  p.with_options(required: true) do |q|
    m << p.text_field_block(:uid, "ID")
    m << p.text_field_block(:email, "メールアドレス")
  end
end %>