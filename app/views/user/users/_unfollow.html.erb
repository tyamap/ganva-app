<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, url: :user_relationships) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>